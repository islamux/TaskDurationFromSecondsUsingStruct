#include <iostream>
using namespace std;

void PrintResult(int days, int hours, int minutes, int seconds){

    cout << days << ":" << to_string(hours) << ":" <<to_string (minutes) << ":" << to_string(seconds) <<"\n";
}